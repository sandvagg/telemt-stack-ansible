# requirements.yml
# Install: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.4.0,<5.0.0"
  - name: community.general
    version: ">=8.0.0,<13.0.0"
